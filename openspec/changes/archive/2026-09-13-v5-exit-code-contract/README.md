# v5-exit-code-contract

v5.0.0 release: fix exit-code contract, enforce release checksums, unify action preset handling, plus low-risk cleanups (remove stale validate-release.sh, fix README defaults, dedupe CHANGELOG, fix action.yml double --bump-type call)
