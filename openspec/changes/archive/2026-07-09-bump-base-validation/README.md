# bump-base-validation

v5.0.1: grubble fails (instead of silently using the file version) when the package file's version is ahead of the latest git tag. Add CONTRIBUTING note about not manually bumping the file.
